{
  "description": "Positive fixtures - clean Python code passes lint",
  "fixtures": [
    {
      "name": "clean_single_function",
      "description": "Simple clean function with no violations",
      "input": {
        "completions": ["def hello():\n    return \"world\"\n"],
        "ground_truth": { "max_violations": 0 }
      },
      "expected": { "verdict": "PASS", "min_score": 0.9 }
    },
    {
      "name": "clean_with_imports",
      "description": "Code using standard library import with no violations",
      "input": {
        "completions": ["import os\n\nprint(os.getcwd())\n"],
        "ground_truth": { "max_violations": 0 }
      },
      "expected": { "verdict": "PASS", "min_score": 0.9 }
    },
    {
      "name": "violations_under_threshold",
      "description": "Code with violations below the allowed threshold still passes",
      "input": {
        "completions": ["import os\nimport sys\n\nprint('hello')\n"],
        "ground_truth": { "max_violations": 10 }
      },
      "expected": { "verdict": "PASS", "min_score": 0.5 }
    }
  ]
}
